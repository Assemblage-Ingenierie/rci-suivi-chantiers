# Variables d'environnement de PRODUCTION (build Vercel).
# Ces deux valeurs sont PUBLIQUES par conception (préfixe NEXT_PUBLIC_, donc
# inlinées dans le bundle navigateur). La sécurité des données repose sur les
# politiques RLS Supabase, pas sur le secret de ces clés — il est donc sûr de
# les versionner. NE JAMAIS mettre la clé service_role ici.
#
# En local, .env.local (gitignoré) a la priorité et prend le relais.
NEXT_PUBLIC_SUPABASE_URL=https://cvdeckwvhowasdefzqlr.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_Lx7wr8v0JlqQJGyxocHbZA_tKOQbWJ8
